# Copy to .env and fill in. Never commit the real file — .env is gitignored.

# Shared secret used to verify the HMAC-SHA256 signature WorkWave appends to
# its webhook callback URL. Must match the secret configured on the WorkWave
# side, or every callback is rejected.
CALLBACK_SHARED_SECRET=replace-me
